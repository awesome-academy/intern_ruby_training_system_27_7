<% provide :title, t(".heading") %>
<% provide :button_text, t(".save_changes") %>

<section class="content-header">
  <h1><%= t ".heading" %></h1>
</section>

<section class="content container">
  <div class="card card-info">
    <div class="card-header">
      <h3><%= t ".edit_user_information" %></h3>
    </div>
    <div class="card-body">
      <%= render "users/user_form", user: @user, path: user_path %>
    </div>
  </div>
</section>
